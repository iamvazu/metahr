<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  
  # Ensure clean URLs work (don't 404 on sub-pages)
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-l
  RewriteRule ^ /index.html [L]
</IfModule>

