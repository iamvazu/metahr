---
description: Run the MetaHR frontend application
---
1. Navigate to the `frontend` directory.
2. Run `npm run dev` to start the Vite development server.
3. The application will be available at `http://localhost:5173`.
